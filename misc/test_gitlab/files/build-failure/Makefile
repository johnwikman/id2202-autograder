.PHONY: all clean

# Deliberately broken, so that the suite can check that a submission which does
# not build is reported as a failed submission rather than as an autograder
# error.
all:
	@echo "this solution does not build" >&2
	@exit 1

clean:
	$(RM) -rf _build hello
