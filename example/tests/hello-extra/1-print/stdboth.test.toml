description = """
Checks that hello world is printed to both stdout and stderr.
"""

[test.options]
args = ["--stdout", "--stderr"]
stdout = ["Hello, World!"]
stderr = ["Hello, World!"]

# This should be ignored
stdin = "bla bla bla"
stdin_ignore = false
