title = "Print"
description = "Prints output"

[test]
kind = "run"

# 5 seconds should be plenty here, useful to test timeout functionality.
timeout = 5

[test.options]
code = [0]
stdin_ignore = true
stdout_trim = true
stderr_trim = true
