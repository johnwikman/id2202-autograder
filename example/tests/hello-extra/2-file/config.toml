title = "File Input"
description = "Handles input files"

# Same stdin used for all tests
[test]
kind = "run"

[test.options]
code = 0
ignore_stdin = true
ignore_stdout = false
ignore_stderr = true
trim_stdout = true
